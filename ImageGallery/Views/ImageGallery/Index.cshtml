@model ImageGallery.Models.GalleryModel
@{
}
@if (Model.Images.Count() == 0)
{
    <div class="text-left">
        <label>No images</label>
    </div>
    <a class="btn btn-primary" asp-area="" asp-controller="Image" asp-action="Upload">Upload new Image</a>
}
<environment names="development">
    <script src="~/lib/jquery/dist/jquery.js"></script>
</environment>
<environment names="proudction">
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
</environment>
<button class="btn btn-primary" onclick='window.location.reload(true)'>Обновить</button>
<div class="container body-content">
    <div class="row gallery-content">
        @foreach (var Image in Model.Images)
        {
            <div class="gallery-image-container">
                <a asp-controller="ImageGallery" asp-action="Detail" asp-route-id="@Image.Id">
                    <div class="gallery-image" style="background-image:url(@Image.Url)"></div>
                </a>
            </div>
        }
    </div>
</div>
