@model ImageGallery.Models.UploadModel
@using ImageGallery.Controllers
<div class="bg-primary m-1 p-1 text-white"><h4>Upload new image</h4></div>
<div class="container body-content ">
    <div class="">
        <div class=" ">
            <form asp-action="AddFile" asp-controller="Image" method="post" enctype="multipart/form-data">
            <div class="form-group">
            <label asp-for="Title"></label>
            <input asp-for="Title" class="form-control" />
        </div>
            <div class="form-group">
            <label asp-for="Tags"></label>
            <input asp-for="Tags" class="form-control" />
        </div>
            <div>
            <label class="btn btn-file">
                <input type="file" name="uploadedFile" id="uploadedFile" style="display: marker" />
            </label>
        </div>
        <div class="simbit">
            <label class="btn btn-outline-success btn-file">
                <input type="submit" id="btn-upload" class="btn btn-info" />
            </label>
        </div>
        @*<div>
            <button asp-controller="ImageController" asp-action="AddFile" type="submit" id="btn-upload" class="btn btn-outline-success">Simbit</button>
        </div>*@
        </form>
        </div>
    </div>
</div>

